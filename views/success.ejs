<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submission Successful</title>
    <link rel="stylesheet" href="/css/mineaid-styles.css">
    <link rel="shortcut icon" href="favicon.png" type="image/x-icon">
</head>
<body id="success_page" class="full-height">
    <header>
        <div class="container">
            <div class="logo">
                <a href="/"><img src="/images/first-aid-kit.png" alt=""></a>
            </div>
            <nav>
                <a href="/">Home</a>
                <a href="/triage">Add New Record</a>
                <a href="/form">Add Incident</a>
            </nav>
        </div>
    </header>
    
    <main>
        <div class="container">
            <!-- Display flash messages if there are any -->
            <% if (success_msg) { %>
                <div class="alert alert-success"><%= success_msg %></div>
            <% } %>
            
            <% if (error_msg) { %>
                <div class="alert alert-danger"><%= error_msg %></div>
            <% } %>
            
            <% if (error) { %>
                <div class="alert alert-danger"><%= error %></div>
            <% } %>
            <div class="logo-large">
                <img src="/images/first-aid-kit.png" alt="">
            </div>
            <section class="intro">
                <h1>Form Submitted Successfully!</h1>
                <h3>Your record has been added.</h3>
            </section>
            <!-- Button Section -->
            <div class="qlinks btn-grp">
                <a href="/" class="btn">Go Home</a>
                <a href="/triage" class="btn">Add Another Record</a>
                <a href="/form" class="btn">Add Incident</a>
                <a href="/records" class="btn">View Records</a>
            </div>
        </div>
    </main>

    <footer>
        <p>&copy; 2024 First Aid Portal. All Rights Reserved.</p>
    </footer>
</body>
</html>
